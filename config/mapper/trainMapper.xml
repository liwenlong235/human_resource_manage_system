<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.dao.TrainDao">
    <insert id="add" parameterType="Train">
        insert into train(title,e_id,start_time,end_time,result) values(#{title},#{eId},#{startTime},#{endTime},#{result})
    </insert>
    <update id="update" parameterType="Train">
        update train set title=#{title},e_id=#{eId},start_time=#{startTime},end_time=#{endTime},
        result=#{result} where t_id=#{tId}
    </update>
    <delete id="deleteById" parameterType="int">
        delete from train where t_id=#{id}
    </delete>
    <select id="queryById" resultType="Train">
        select * from train where t_id=#{id}
    </select>
    <select id="queryByEId" resultType="Train">
        select * from train where e_id=#{eId}
    </select>
    <select id="queryAll" resultType="Train">
        select * from train
    </select>
</mapper>